"use strict";
// 타입을 지정해놓고, 타이핑량을 줄일 수 있다.
let another = 0;
let another2 = ["myNumber", 7];
const fucTest = (name) => {
    return;
};
// 타입의 존재가치가 명확하면 인터페이스를 사용하는 편
// 대상을 가리킬 뿐이던가, 그저 별명으로써만 존재를 한다면 type alias를 사용한다.
